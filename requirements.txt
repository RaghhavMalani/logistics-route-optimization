# India PortWatch -- core runtime.
# Everything the pipeline, the API and the test suite need. The deep-learning
# stack is deliberately optional: the adaptive ensemble runs on persistence +
# the GBM quantile model without it, and the benchmark reports the TFT as
# not evaluated rather than pretending otherwise.

# --- data + modelling -------------------------------------------------------
pandas>=2.0
numpy>=1.23
scikit-learn>=1.4
hmmlearn>=0.3          # HSMM backend; falls back to a GaussianMixture if absent

# --- live connectors --------------------------------------------------------
requests>=2.31         # IMF PortWatch, Open-Meteo, GDELT, FRED

# --- API --------------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
httpx>=0.27            # required by fastapi.testclient

# --- Optional feature stacks ------------------------------------------------
# Real Temporal Fusion Transformer:  pip install -r requirements-tft.txt
# Dashboard / storage / plots:       pip install -r requirements-extras.txt
# Browser UI smoke test:             pip install playwright && playwright install chromium
